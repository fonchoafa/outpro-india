[build]
  command = "npm install --prefix frontend && npx --prefix frontend vite build"
  publish = "frontend/dist"
  functions = "api"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/index/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
